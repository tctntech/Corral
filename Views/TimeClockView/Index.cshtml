<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Time Clock</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            background-color: #ffffff;
            padding: 20px; /* Reduced padding */
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px; /* Reduced max-width */
            text-align: center;
        }

        .header h1 {
            font-size: 1.5rem; /* Reduced font size */
            color: #007bff;
            font-weight: 500;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 0.9rem; /* Reduced font size */
            color: #6c757d;
            margin-bottom: 20px;
            line-height: 1.5;
        }

        /* Button Section */
        .button-section {
            display: flex;
            flex-direction: column;
            gap: 10px; /* Reduced gap */
            margin-top: 20px;
        }

        .btn-custom {
            font-size: 0.9rem; /* Reduced font size */
            padding: 10px; /* Reduced padding */
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            font-weight: 500;
            outline: none;
        }

        .btn-primary-custom {
            background-color: #007bff;
            color: #ffffff;
            box-shadow: 0 4px 12px rgba(0, 123, 255, 0.2);
        }

            .btn-primary-custom:hover {
                background-color: #0056b3;
                box-shadow: 0 6px 18px rgba(0, 123, 255, 0.3);
                transform: translateY(-2px);
            }

        .btn-secondary-custom {
            background-color: #6c757d;
            color: #ffffff;
            box-shadow: 0 4px 12px rgba(108, 117, 125, 0.2);
        }

            .btn-secondary-custom:hover {
                background-color: #5a6268;
                box-shadow: 0 6px 18px rgba(108, 117, 125, 0.3);
                transform: translateY(-2px);
            }

        .footer {
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <div class="container">
        <!-- Header Section -->
        <div class="header">
            <h1>Time Clock</h1>
            <p>
                Welcome to the Time Clock system. Use the options below to manage shifts, view staff, or perform administrative tasks. For assistance, contact the administrator.
            </p>
        </div>

        <!-- Buttons Section -->
        <div class="button-section">
            <button class="btn-custom btn-primary-custom" onclick="window.location.href='/TimeClock/New'">
                Open New Time Clock
            </button>
            <button class="btn-custom btn-primary-custom" onclick="window.location.href='/TimeClock/ShiftsByPaycycle'">
                Shifts by Paycycle
            </button>
            <button class="btn-custom btn-primary-custom" onclick="window.location.href='/TimeClock/ClockedInStaff'">
                View Clocked-In Staff
            </button>
            <button class="btn-custom btn-primary-custom" onclick="window.location.href='/TimeClock/AdminFeatures'">
                Admin Features
            </button>
        </div>

        <!-- Footer Section -->
        <div class="footer">
            <button class="btn-custom btn-secondary-custom" onclick="window.location.href='/AdminDashboard'">
                Close
            </button>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
